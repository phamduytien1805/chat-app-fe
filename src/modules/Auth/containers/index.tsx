import { Box, Grid } from '@mui/material';
import { FC } from 'react';

interface Props {}

export const AuthenticationPage: FC<Props> = (props) => (
  <Grid container>
    <Grid item>
      <Box>{'asdasdasd'}</Box>
    </Grid>
    <Grid item />
  </Grid>
);
