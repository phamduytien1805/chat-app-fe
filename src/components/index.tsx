import React from 'react';
import CButton from './Button';
type Props = {};

const Demo = (props: Props) => (
  <div>
    <CButton variant='contained'>{'asdasd'}</CButton>
  </div>
);

export default Demo;
